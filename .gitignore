# Python bytecode (bin/prompt-chat-runner.py compiles to __pycache__/)
__pycache__/
*.pyc

# Env / secrets
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Plugin output — per-run dirs (.voicepromptkit/<prompt>/run-NNN/) and the
# content-addressable cache (.voicepromptkit/_cache/<key>/{lens}.json).
.voicepromptkit/

# Notes worktree
/notes/

# Claude Code local-only settings
.claude/settings.local.json

# >>> sync-notes managed entries >>>
# Notes worktree (tracked in notes branch)
/.notes
/notes/
# <<< sync-notes managed entries <<<

# obsidian-memory worktree symlink
.memory
